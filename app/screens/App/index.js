'use strict';

var React = require('react');

require('normalize-css/normalize.css');

var App = React.createClass({
  render: function() {
    return (
      <div className="app">
        App!
      </div>
    );
  }
});

module.exports = App;
